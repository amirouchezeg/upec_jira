----front
    --// admin dashboard
    --projects
        -- disable button save
        -- edit project
        -- delete project
        -- show the projects per page
    --navar
        -- switch between navbars when user is connecte or no
        </> add avatar with profil,chage password,disconect
        </> show snackBar when is disconected
    --sprint details page  
        --line it with back
            -- get and show all issues
            -- get issue of curant sprint
            -- edit issue
            -- delete issue
    -- issues
        -- edit sprint
        -- delete sprint
    </> when user go to any url check it is authoriazed
    -- when connect refrech page 
    -- profile
        -- edit profile
        -- show email in profile and disable to edit it
        -- add link to edit the password
----back
    -- edit password (send : email, old_password, and new_password)
    -- change the paths 
        -- ex for sprint is: api/projects/:id/sprints/:id
    -- add token for the importante information
    --projects
        </> add or delete memebers of projects (to make it now it shold to send all old and the new one in edit project )
        --// show by pages (by 20) todo only if it is easy
    --spring 
        -- check when chage stat to in_progresse that thier not auther in same status
    --issues 
        -- **edit  the user email of this issue (asigne to auther user)
    --// check if it is authoriazed to get this paths 
    -- email
        -- designe the page of mail sended
        -- send chack email (check email)
        -- create project or adding personne to project send in email the link of project 
--------------documentation
    --complite the swager
    --write texts for the web site
